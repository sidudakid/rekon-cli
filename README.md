# rekon-cli
## Installation
```bash
git clone https://github.com/sidudakid/rekon-cli.git
cd rekon-cli/
sudo mv rekon ~/usr/bin/
```

## About
This is the first release of rekon-cli which is automation tool for Recon in bugbounty which use tools and regex with all file and folder management
